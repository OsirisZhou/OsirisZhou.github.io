export interface ProjectItem {
    id: string;
    name: string;
}

export interface SocialLink {
    platform: string;
    url: string;
    iconClass: string;
}

export interface SkillItem {
    id: string;
    name: string;
}
